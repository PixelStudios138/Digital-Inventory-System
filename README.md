# Digital-Inventory-System

This is a simple inventory system, using classes to create and manage products. You can view, add, restock, and sell products from within your inventory.
It uses 2 classes - `Product()` & `Inventory()` to work.
